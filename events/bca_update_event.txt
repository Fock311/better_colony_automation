namespace = bca_update_event




#initalization
event = {
	id = bca_update_event.2

	is_triggered_only = yes
	hide_window = yes

	trigger = {
		always = yes
	}

	immediate = {
		every_playable_country = {
			limit = { is_ai = no }
            every_owned_planet = {
				limit = {
					NOT = {
				    has_planet_flag = bca_pf_ps_initialized
              		}
				}
				planet_event = { id = colony_automation_event.26 }
			}
        }
	}
}